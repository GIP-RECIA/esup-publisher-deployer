dependencies {
    compileOnly "org.esupportail.publisher:esup-publisher-ui:${esupPublisherVersion}@war"
}

apply plugin: 'war'

war {
    archiveFileName="${appName}.war"

    classpath = classpath.files
}